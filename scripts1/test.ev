global {
	var globVar1 = "GLOBAL1";
}

td.count == 1 : {
	print("8. " + globVar);
	print("9. " + add(1,2));
}

td.count > 50 : {
	td.count = 0;
}

td.count : {
	td.zone(1).active = !td.zone(1).active;
}

td.zone(1).active : {
	print("Active: " + _e.value);
}

td.zone : {
	if (_e.id.endsWith("active")) {
		print("zone " + _e.zoneIndex + " active: " + _e.value);
		print("zone " + _e.getZoneIndex() + " active: " + _e.getValue());
	}
} 


/*
mydr.dp1 == true && knx.dimmer.a1 > 50 || clock.simpleTime > '0800' : { if (true) { var a = 0; } } 
mydr.dp1 == true && knx && dmx || !dmx && !clock.simpleTime > "0800" : { print('Hello, World\n'); }
mydr.dp1 == true : { print("Hello, World\n"); }
mydr.dp1 == true && knx.dimmer.a1 > 50 || tck && clock.simpleTime > 0800 : { print('Hello, World\n'); }
mydr.dp1 == true && knx.dimmer.a1 > 50 || tck && clock.simpleTime > 0800 : { print('Hello\n'); }


mydr.dp1 == "dsd" : { 
	if (skjdks) var a = "}";
	
		
	sjdhjs;
	// ajksl {asodjkod
	sjs;
	//{ 
}
*/

/*
mydr.text : {
	print('ret: ' + myFunction(3,5) + '\n');
	print('var: ' + myVar + '\n');
}

mydr : {
	if (ev instanceof com.sfera.events.NumberEvent) {
		print('num: ' + _e.getValue() + '\n');
	}
	print('ev: ' + _e.getValue() + '\n');
	mydr.doSomething("it");
}
*/